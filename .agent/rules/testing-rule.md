---
trigger: model_decision
description: when you are developing next js code
---

always do unit test for backend
always do unit test for frontend
always do npm run build and make sure there are no errors
always check dev sever (npm run dev) logs and make sure there are no error
always use the browser to test UI if you hit rate limit report it and move on
if DB exists always test DB operation for the features you are creating
return a full test report as artifact
